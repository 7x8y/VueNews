module.exports = {
    HOST: "localhost",
    USER: "root",
    PASSWORD: "redhat",
    DB: "m4"
  };

  // CREATE TABLE IF NOT EXISTS `users` (     id int(11) NOT NULL PRIMARY KEY AUTO_INCREMENT, 
  //  imageurl varchar(500) ,
  //   posttitle varchar(255) NOT NULL,    
  //    postbody text NOT NULL,     
  //    active BOOLEAN DEFAULT false   );